import React from "react";

import Details from "../../components/Orgs/Details";

export default {
  title: "Organization/Details",
  component: Details,
};

const Template = (args) => <Details {...args} />;

export const Primary = Template.bind({});
